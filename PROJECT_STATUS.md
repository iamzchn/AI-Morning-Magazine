# AI 晨读：项目状态

最后核对：2026-07-27（Asia/Shanghai）

## 一句话目标

每天自动筛选少量可靠的 AI / 模型 / Agent / 开源资讯，以中文卡片网页阅读，并保留原文链接。

## 项目边界

| 目录 | 角色 | 是否是用户阅读入口 |
| --- | --- | --- |
| `D:\AI-Morning-Magazine` | 主项目：网页、网页数据、构建产物 | 是 |
| `D:\AI-News-Horizon` | 后台：抓取 RSS、调用 DeepSeek 筛选与生成摘要 | 否 |

不要在两个目录分别维护“两个产品”。Horizon 只负责生成数据；晨读网页才是最终成品。

## 当前已完成

- 网页项目能够构建：已验证 `npm run build` 成功。
- Horizon 已安装依赖，并配置 DeepSeek：密钥只保存在 `D:\AI-News-Horizon\.env`，不进入 Git。
- Horizon 每日计划任务已创建：`AI-News-Horizon Daily`，每天 08:00 运行。
- 每日脚本会依次运行 Horizon、`scripts/import_horizon.py`、再构建晨读网站。
- 最新 Horizon 摘要会被转换到 `public/data/latest.json`，供网页读取。
- 用户可通过 `打开AI晨读网站.cmd` 启动本地预览，浏览地址为 `http://127.0.0.1:8765`。
- 正式网站已发布：`https://ai-morning-magazine-15541.siisisisi.chatgpt.site`。
- 网页数据已升级为中英字段：每条保留中文标题/解读、英文标题/摘要、原文链接与 AI 分数；页面同时展示高分头条和全部已导入条目。

## 已知状态与限制

- 目前已经生成过一份 `horizon-2026-07-27-en.md`。它是旧配置生成的英文、泛技术内容，只用来验证“摘要 → 网页数据”的转换链路。
- Horizon 已改为后续生成中文，并禁用容易混入非 AI 内容的 Hacker News 与 OSS Insight；下一次成功运行后应产出中文 AI 资讯。
- 2026-07-27 手动验收结果：Horizon 从配置的 RSS 源抓到 0 条内容，因此没有生成 `*-zh.md`，也没有调用 DeepSeek。每日脚本仍把旧英文摘要再次导入网页；这意味着“中文摘要 → 网页”的真实链路尚未通过，不能视为完成。
- `public/data/latest.json` 已用 Python 原样验证为合法 JSON，包含 11 条旧英文内容；PowerShell 的中文显示乱码不代表该文件损坏。
- RSS 只读诊断结论：OpenAI News、Hugging Face Blog、GitHub Blog 均返回 HTTP 200，且可被 feedparser 与 Horizon 的 `RSSScraper` 正常解析。三者最新条目均为 2026-07-23；本次 24 小时窗口从 2026-07-26 开始，故全部被时间过滤排除。用 Horizon 的同一解析器以 2026-07-20 为起点可取得 18 条内容，排除解析故障。
- 当前是本机可打开的本地网站，不是公开互联网网站。若要让手机或其他电脑访问，仍需单独完成 GitHub Pages / Cloudflare Pages 等线上部署。
- 计划任务尚未到下一次自动执行时间；不要把“任务已创建”误认为“新配置已被自动验证”。
- 已完成一次性 7 天首期回填：12 条高分资讯已生成中英双语配对并导入网页；页面会完整显示这 12 条，而非只展示头条。后续运行已配置为生成 `zh` 和 `en` 两份对应摘要，导入脚本会按原文链接配对。

## 下一步（只做这一件）

决定每日空窗的产品策略：保持“没有新条目就不发布”，或增加来源以提高每日有内容的概率。最小的首次验收操作建议是**仅一次**以 7 天窗口生成中文首期；不要直接把日常窗口永久改为 7 天，否则会每天重复摘要相同资讯。

修复并重新验收前，不进入线上部署阶段，也不要增加推送、登录、数据库或多 Agent 功能。

## 新任务的固定开场白

> 先读取 `AGENTS.md` 和 `PROJECT_STATUS.md`，总结当前事实与下一步；未经我确认不要改变项目边界或新增功能。
