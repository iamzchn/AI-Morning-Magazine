# AI 晨读：项目协作说明

## 项目目标

做一个给个人使用的「AI 晨读」网页：每天用 5 分钟读懂值得关注的 AI、模型、Agent 与开源动态。内容必须少而精，并保留原文链接。

## 当前阶段

当前优先级是跑通最小闭环，不扩展新功能：

1. Python 脚本 `collect_news.py` 从少量官方 RSS 获取资讯。
2. 脚本生成网站可读取的 `public/data/latest.json`。
3. 前端页面读取并展示真实数据，而不是样例数据。
4. `npm run build` 通过；部署后的站点能正常显示最新数据。

目前不追求资讯来源数量。先用 OpenAI、Hugging Face、GitHub Blog 等少量来源稳定验证。

## 之后的路线（按顺序）

1. 先手动验证抓取 → 数据文件 → 网页展示的完整链路。
2. 再接入模型 API，对原始资讯做去重、分类、筛选与中文解读。
3. 再将代码推送到 GitHub，用 GitHub Actions 每日定时运行抓取/处理/部署。
4. 只有上述流程稳定后，才考虑提醒推送、微信小程序、OpenClaw、多 Agent、登录或复杂搜索。

## 未来模型处理的输出规范

每条入选资讯应有：

- 标题与来源
- 原文链接
- 发生了什么（简短、中文）
- 为什么重要（面向 AI/Agent 初学者）
- 分类：模型 / Agent / 产品 / 开源 / 人物观点
- 重要性：1–5

模型不得编造事实；不确定的信息应保守表达并保留原文链接。

## 工作方式

- 一次只解决一个可验证的问题；完成后报告验证结果。
- 修改前先阅读现有文件，避免无关重写。
- 不要因为“更像 Agent”而过早引入多 Agent、OpenClaw、数据库或多个完整开源系统。
- 涉及 API 密钥时，不要把密钥写进代码、JSON 或 Git；使用环境变量，并确保 `.gitignore` 覆盖本地密钥文件。
- 对部署改动，至少验证 `npm run build`；对抓取改动，至少执行一次脚本并检查输出数据。

## 当前下一步

停止增加功能，优先完成构建与部署验证：确认 `npm run build` 成功、已部署网页可打开、真实 RSS 数据已显示。
